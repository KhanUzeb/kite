name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  pytest:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11", "3.12"]
    runs-on: ${{ matrix.os }}
    env:
      KITE_HOME: ${{ runner.os == 'Windows' && 'C:\\Temp\\kite-home' || '/tmp/kite-home' }}
      KITE_SKIP_SETUP: "1"
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true

      - name: Install and test
        shell: bash
        run: |
          uv pip install -e ".[dev]"
          python scripts/sync_version.py --check
          uv run ruff check src tests
          uv run pytest -q
