## Memory layers

One layer per fact — narrowest scope wins: **project** (`AGENTS.md` / `KITE.md`, not the memory tool) → **user** (`USER.md` / `PROFILE.md`) → **session** (`MEMORY.md`, episodic, `/remember`). No secrets. Continuity blocks are not durable memory.
